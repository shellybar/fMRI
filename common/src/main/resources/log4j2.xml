<?xml version="1.0" encoding="UTF-8"?>
<Configuration>

    <Properties>
        <Property name="log-pattern">%d{yyyy-MM-dd HH:mm:ss} %-5level [%t] %c{1} - %msg%n</Property>
    </Properties>

    <Appenders>
        <RollingFile name="file-log" fileName="${sys:log.directory}/server.${date:yyyy-MM-dd}.log"
                     filePattern="${sys:log.directory}/server.%d{yyyy-MM-dd}.log"
                     immediateFlush="false" append="true">
            <PatternLayout pattern="${log-pattern}"/>
            <Policies>
                <TimeBasedTriggeringPolicy interval="1" modulate="true"/>
            </Policies>
        </RollingFile>
        <Console name="console" target="SYSTEM_OUT">
            <PatternLayout pattern="${log-pattern"/>
        </Console>
    </Appenders>

    <Loggers>
        <Logger name="edu.tau.eng.neuroscience.mri" level="debug" additivity="false">
            <AppenderRef ref="console"/>
            <AppenderRef ref="file-log"/>
        </Logger>
        <Root level="error">
            <AppenderRef ref="console"/>
            <AppenderRef ref="file-log"/>
        </Root>
    </Loggers>

</Configuration>
